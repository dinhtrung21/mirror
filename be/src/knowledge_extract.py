from typing import List


def extract_knowledge(message: str) -> List[str]:
    """Use TF-IDF to extract keyword

    Args:
        message (str): input message sent by client

    Returns:
        List[str]: list of knwoledge in string
    """
    return ["", ""]
